
/// Holds all information on the game world.
pub struct World {}

impl World {

    /// Creates a new world.
    pub fn new() -> Self {
        World {}
    }
}

impl Default for World {

    /// Creates a default world.
    fn default() -> Self {
        World {}
    }
}
